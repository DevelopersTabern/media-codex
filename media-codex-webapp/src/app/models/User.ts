import { Title } from '@angular/platform-browser';

export interface User {
    id?: number;
    userName?: string;
    password: string;
    fisrtName: string;
    lastName: string;
    email: string;
    address: string;
};